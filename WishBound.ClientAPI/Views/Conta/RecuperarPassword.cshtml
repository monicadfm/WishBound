@model RecuperarPasswordViewModel
@{
    ViewData["Title"] = "Recuperar password";
}

<div class="autenticacao-cartao">
    <h1>Recuperar password</h1>
    <p class="texto-suave">
        Indique o email da conta. Será gerado um link para definir uma nova password.
    </p>

    <form class="formulario formulario-autenticacao" method="post" asp-controller="Conta" asp-action="RecuperarPassword">
        <div asp-validation-summary="ModelOnly" class="alerta alerta-erro alerta-validacao"></div>

        <div class="campo">
            <label asp-for="Email"></label>
            <input asp-for="Email" placeholder="Ex.: nome@email.com" autofocus />
            <span asp-validation-for="Email" class="erro-campo"></span>
        </div>

        <div class="formulario-acoes">
            <button type="submit" class="botao botao-destaque">Gerar link de recuperação</button>
            <a class="botao botao-suave" asp-controller="Conta" asp-action="Login">Voltar ao login</a>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
